# Optional: SQLite DB file (default: <repo>/data/database.sqlite)
SQLITE_PATH=
# Optional: permanent media root (default: <repo>/data/media)
MEDIA_PATH=
# Optional: temp uploads (default: <repo>/data/temp)
MEDIA_TEMP_PATH=